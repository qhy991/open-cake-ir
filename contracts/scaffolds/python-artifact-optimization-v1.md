# Python artifact optimization

Use the supplied Workload Contract and Python starter to propose structurally distinct
Schedules for the declared exact target and shape. Preserve Workload metadata, argument
order, tensor shapes, numerical semantics and output ownership. Submit each Python source
as a `python_source` member of the candidate-set envelope named by the StateCard.

Read localized Compiler and Evaluation feedback before the next turn. The common Lab
owns compilation, reference checks across every declared input case, timing, profiling
and stopping. Do not invoke those tools, a GPU, the network or another task. Change only
the named candidate envelope; keep TASK.md and AGENTS.md unchanged. This task may establish
a locally confirmed artifact, with no arm-comparison or cross-shape claim.
