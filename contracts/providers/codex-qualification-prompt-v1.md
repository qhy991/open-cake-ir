CANDIDATE_PATH_JSON={{CANDIDATE_PATH_JSON}}
This is a zero-GPU provider capability qualification.
Use exactly one file change to {{EXPECTED_CHANGE}} the candidate path above.
Read the embedded frozen reference bundle and copy its qualification nonce.
Write exactly this JSON object: {{EXPECTED_CANDIDATE_JSON}}
REFERENCE_BUNDLE_SHA256={{REFERENCE_BUNDLE_SHA256}}
{{REFERENCE_BUNDLE}}
Do not create any other file, invoke a GPU, use the network, or run a compiler.
After the file change, return only the requested output-schema object.
