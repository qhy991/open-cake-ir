# Generated by open-cake-ir from gemm-bias-b1-smoke-v1; DO NOT EDIT.
# schedule_sha256=dc7cae4f20f48a636e30c6d6056a35c8722dc06bcd7c5a95e98015bf57bcbaac
from __future__ import annotations

import torch
import triton
import triton.language as tl


@triton.jit
def _cake_gemm_bias_b1_smoke_kernel(
    a,
    b,
    bias,
    c,
    N_M_BLOCK: tl.constexpr,
    N_K_LOOP: tl.constexpr,
    N_N_BLOCK: tl.constexpr,
    D_B_1: tl.constexpr,
    D_BIAS_0: tl.constexpr,
    D_C_0: tl.constexpr,
    D_C_1: tl.constexpr,
    BLOCK_M_BLOCK: tl.constexpr,
    BLOCK_N_BLOCK: tl.constexpr,
    BLOCK_K_LOOP: tl.constexpr,
    NUM_STAGES: tl.constexpr,
):
    m_block = tl.program_id(0)
    n_block = tl.program_id(1)
    m_block_offsets = m_block * BLOCK_M_BLOCK + tl.arange(0, BLOCK_M_BLOCK)
    n_block_offsets = n_block * BLOCK_N_BLOCK + tl.arange(0, BLOCK_N_BLOCK)

    acc = tl.zeros((64, 64), tl.float32)

    for k in tl.range(0, N_K_LOOP, BLOCK_K_LOOP, num_stages=2, disallow_acc_multi_buffer=True):
        k_offsets = k + tl.arange(0, BLOCK_K_LOOP)
        # CAKE_OP:load_a
        load_a_ptrs = a + m_block_offsets[:, None] * N_K_LOOP + k_offsets[None, :]
        a_tile = tl.load(
            load_a_ptrs,
            mask=(m_block_offsets[:, None] < N_M_BLOCK) & (k_offsets[None, :] < N_K_LOOP),
            other=0.0,
            cache_modifier=".cg",
        )
        # CAKE_OP:load_b
        load_b_ptrs = b + n_block_offsets[:, None] * D_B_1 + k_offsets[None, :]
        b_tile = tl.load(
            load_b_ptrs,
            mask=(n_block_offsets[:, None] < N_N_BLOCK) & (k_offsets[None, :] < D_B_1),
            other=0.0,
            cache_modifier=".cg",
        )
        # CAKE_OP:dot
        acc += tl.dot(a_tile, tl.trans(b_tile))

    # CAKE_OP:load_bias
    load_bias_ptrs = bias + n_block_offsets
    bias_tile = tl.load(
        load_bias_ptrs,
        mask=n_block_offsets < D_BIAS_0,
        other=0.0,
        cache_modifier=".cg",
    )
    # CAKE_OP:add_bias
    c_tile = acc + bias_tile[None, :]

    # CAKE_OP:store_c
    tl.store(
        c + m_block_offsets[:, None] * D_C_1 + n_block_offsets[None, :],
        c_tile,
        mask=(m_block_offsets[:, None] < D_C_0) & (n_block_offsets[None, :] < D_C_1),
    )
    # CAKE_KERNEL_END


def cake_gemm_bias_b1_smoke(a, b, bias, out=None):
    for tensor, shape, dtype in (
        (a, (512, 256), torch.bfloat16),
        (b, (256, 256), torch.bfloat16),
        (bias, (256,), torch.float32),
    ):
        if tuple(tensor.shape) != shape:
            raise ValueError("an input differs from the frozen shape")
        if tensor.dtype != dtype:
            raise TypeError("an input differs from the frozen dtype")
        if not tensor.is_cuda or not tensor.is_contiguous():
            raise ValueError("every input must be contiguous on CUDA")
    if any(t.device != a.device for t in (a, b, bias,)):
        raise ValueError("every input must share one device")
    if out is None:
        out = torch.empty((512, 256), dtype=torch.float32, device=a.device)
    if tuple(out.shape) != (512, 256) or out.dtype != torch.float32:
        raise ValueError("out differs from the frozen output contract")
    if out.device != a.device or not out.is_contiguous():
        raise ValueError("out must be contiguous on the input device")
    _cake_gemm_bias_b1_smoke_kernel[(8, 4, 1)](
        a,
        b,
        bias,
        out,
        N_M_BLOCK=512,
        N_K_LOOP=256,
        N_N_BLOCK=256,
        D_B_1=256,
        D_BIAS_0=256,
        D_C_0=512,
        D_C_1=256,
        BLOCK_M_BLOCK=64,
        BLOCK_N_BLOCK=64,
        BLOCK_K_LOOP=64,
        NUM_STAGES=2,
        num_warps=4,
        num_stages=2,
        maxnreg=128,
    )
    return out
