# open-cake-ir 导览（2026-09-18 快照）

`index.html` 是一页面向技术读者的项目介绍：项目结构、执行计划与 Python 前端、三层反馈、
七个硬件目标各自的计时器、2026 年 9 月中文技术报告的章节与两个案例、治理节奏、路线图与当前数字。
它是 [CONTEXT-MAP](../../CONTEXT-MAP.md) 分类里的**历史快照**：内容绑定 `main@c75ee4b3`（2026-09-17）
与技术报告压缩包 `open-cake-ir-technical-report-zh-CN.zip`（release `research-preview-2026-09-06`，
报告版本 2026-W36.1，绑定源码快照 `0d954c5`）。数字随提交变化，当前状态以
[`reports/current/STATUS.md`](../../reports/current/STATUS.md) 为准；需要更正时写后继快照，不改写本页。

- `figures/` 中的四张 PNG 是技术报告的图 1–4 原图（Apache-2.0，随报告压缩包分发）。
- 两张案例图按报告的 `data/report-data.json` 重绘为内联 SVG；Apple 与 B300 的测量转录自 `findings/`；
  无 GPU 的命令输出在 `c75ee4b3` 上实跑。
- 页面只依赖 Google Fonts（可选，缺失时回退系统字体），无其他外部资源；直接用浏览器打开即可。
